import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputLockContainer: {
    backgroundColor: '#3591fa',
    width: '100%',
    color: '#FFFFFF',
    fontSize: 20,
    height: 40,
    borderWidth: 1,
    borderRadius: 8,
    padding: 5,
    textAlign: 'center',
  },
});
